package MyGame;
 

public class Spawn {
	
	private Handler handler;
	private HUD hud;
	
	public Spawn(Handler handler, HUD hud) {
		this.handler = handler;
		this.hud = hud;
	}
	
	public void tick() {
		//changes level in HUD
		//hud.setLevel(hud.getLevel()+1);
			
		//if(hud.getLevel() == 2) {
			
		//}
	}
}